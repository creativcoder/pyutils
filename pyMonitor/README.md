pyMonitor - A simple utiliy to notify the number of bytes send and received on an active internet connection.

Dependencies - libnotify-bin, psutil (python module), notify-send ( binary that uses libnotify api's )
